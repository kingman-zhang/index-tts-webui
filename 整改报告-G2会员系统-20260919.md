# G2 会员系统 · 整改报告

> 2026-09-19 凌晨完成 · commit `628a61a`（分支 feat-single）· 18 文件 +2189 行

## 一、需求对照（你提的 4 条）

| # | 需求 | 状态 | 落点 |
|---|------|------|------|
| 1 | 注册/登录 + 用户自行编辑保存基础信息 | ✅ 完成 | `/account` 页 + `/api/auth/*`、`/api/users/me` |
| 2 | 积分可加、可减、优惠码兑换 | ✅ 完成 | 流水制积分 + 管理端发放/扣减 + 优惠码兑换 + 每日签到 |
| 3 | 积分使用记录 | ✅ 完成 | 每笔变动都有流水（谁/多少/为什么/余额），分页查询 |
| 4 | 其他（自由发挥） | ✅ 见下 | |

**附加做的（第 4 条展开）：**
- **每日签到**送积分（留存抓手）
- **注册赠送**积分（可配置可关闭）
- **合成扣费钩子**：按字数预扣、失败/取消自动全额退款、改稿多退少补——**默认关闭**，`.env` 加 `MEMBER_ENFORCE=1` 即开启收费模式
- **管理接口 + CLI 工具**：建码/发积分/用户列表/禁用账号，无需跑后端也能管
- **改密码**（改完全端登出）、**账号禁用**（禁用即踢下线）
- **Header 用户菜单**：两个工作页右上角显示昵称+积分余额

## 二、怎么用（30 秒上手）

```bash
# 1. 后端 .env 里加一行（现在就生效，无需改代码）
MEMBER_ADMIN_TOKEN=local-admin-token   # 管理口令，不加则管理功能整体禁用

# 2. 生成优惠码（CLI 直改文件，后端不用跑）
cd webui-backend && python tools/member_admin.py add-codes --points 100 --count 10
python tools/member_admin.py users          # 看用户
python tools/member_admin.py grant --user 某人 --delta 500 --reason 补偿

# 3. 前端访问 /account（右上角也有登录入口）
```

想开始收费：`.env` 加 `MEMBER_ENFORCE=1`（未登录不能提交合成、登录用户按 `10 积分/千字` 预扣、失败自动退款）。**不加这行，一切照旧，老用户零感知。**

## 三、关键设计决策（为什么这么做）

| 决策 | 理由 |
|------|------|
| JSON 文件存储（`data/members/`），不引数据库 | 零新依赖，与项目现有存储风格一致；现规模（百级用户）够用，service 层接口不变，日后可平滑换 SQLite |
| pbkdf2 口令哈希 + 独立盐（标准库） | 不存明文；常数时间比较防时序攻击；零依赖 |
| 全部"可选鉴权"设计 | **风险收敛**：不带 token 的请求行为与旧版完全一致，现有双人播客/配音链路零影响 |
| 扣费走"预扣+失败退款"，退款幂等 | 用户不会为失败任务买单；同一笔扣费只退一次（流水 ref 去重），重试会重新预扣 |
| 扣费默认关（ENFORCE=0） | 收费是业务决策，代码先就位、开关你来定 |

## 四、改动清单

**后端新增**（`webui-backend/`）：
- `app/membership/`：`store.py`（JSON 原子落盘）、`security.py`（哈希/token）、`service.py`（全部业务逻辑）、`routes.py`（API）、`__init__.py`
- `tools/member_admin.py`：管理 CLI
- `tests/test_membership.py`（47 项）、`tests/test_points_hook.py`（19 项）
- `MEMBERSHIP.md`：完整使用文档

**后端修改**（最小侵入）：
- `app/routes/__init__.py`：挂载会员路由（+2 行）
- `app/routes/queue.py`：submit/edit/retry/cancel 四处扣费接线（全部被 `member_svc.ENFORCE` 开关包裹）
- `app/queue_worker.py`：两处 `finally` 加自动退款（幂等，异常不外抛）

**前端新增**：
- `src/pages/AccountPage.tsx`：个人中心（登录/注册 tab、资料+改密、签到+兑换、流水分页）
- `src/components/UserMenu.tsx`：Header 右侧用户菜单
- `src/lib/auth.ts`：token 持久化 + 全局登录态（useSyncExternalStore，零依赖）
- `src/api/members.ts`：会员 API 客户端

**前端修改**：
- `App.tsx`：+`/account` 路由（3 行）；`Header.tsx`：+UserMenu（4 行）

## 五、验证结果（全绿）

| 验证 | 结果 |
|------|------|
| `test_membership.py`（注册/登录/资料/改密/流水/兑换/签到/管理守卫/禁用） | **47/47** |
| `test_points_hook.py`（预扣/402 不足/改稿调差/失败退款/取消退款/幂等） | **19/19** |
| 既有回归：mono_pauses / mono_concurrency / registry_order | 全过 |
| 真机 curl 全链路（注册→资料→签到→建码→兑换→流水→未登录提交照常） | 通过 |
| 浏览器真机（vite :6008）：注册→进个人中心→签到+5→兑换+66（105→171）→工作页右上角显示"昵称+171" | 通过 |

## 六、遗留与建议（按优先级）

1. **`MEMBER_ADMIN_TOKEN` 记得换掉**：本地验证用了 `local-admin-token`，上服务器前改成强随机值。
2. **收费单价待定**：默认 10 积分/千字。参考成本：自建 ¥1.1/万汉字、302.ai ¥2.6/万。若 1 积分≈0.01 元，10 积分/千字 = ¥1/千字 = ¥100/万，毛利充足；想更薄可调 `MEMBER_POINTS_PER_1000_CHARS`。
3. **注册赠送/签到数值**：默认 100/5，`MEMBER_REG_BONUS`、`MEMBER_CHECKIN_BONUS` 可调（0 关闭）。
4. 服务器侧部署时把 `data/members/` 纳入备份即可（JSON 纯文本）。
5. 可选后续：积分充值（微信支付）、邀请奖励、兑换码导出 CSV——接口都留好了，加起来很快。

## 七、回滚方式

单 commit `628a61a`，`git revert 628a61a` 即可整体回滚；数据文件在 `data/members/`，与项目数据隔离，回滚代码不影响已有用户数据。

---

# 二轮整改（2026-09-19 晚，commit aeb53b9）

## 需求与决策

| 事项 | 用户决策 | 落地 |
|---|---|---|
| 导航乱 | 方案B：顶部 tab（用户选定，未采用我推荐的侧边栏） | Header 全局导航 tab，三页互通 |
| 注册太简单 | 邮箱注册，QQ SMTP 通道（评估后用户确认） | 验证码流程 + SMTP 直发 |

## 改了什么

**导航（前端 3 文件）**
- `Header.tsx`：logo 右侧新增三个导航 tab（双人播客/单人配音/个人中心），当前页高亮、点击跳转；TTS 状态从"图标+文字+Badge"三重收敛为图标+文字；新增 `showNameInput`/`showTts` 开关
- `AccountPage.tsx`：顶栏从独立实现换成共用 Header（同款 tab），删掉孤立的"返回工作台"按钮

**邮箱注册（后端 4 文件 + 前端 3 文件）**
- 新增 `app/membership/mailer.py`：标准库 smtplib 直发，支持 QQ/163（465 SSL / 587 STARTTLS / none 明文调试），未配置 SMTP_* 时功能整体关闭（503）
- `service.py`：`request_email_code`（6 位码、10 分钟有效、同邮箱 60s/条、每日 10 条、错 5 次作废、发送失败自动作废）+ `register_email`（验证通过才建号发赠送积分；用户名从邮箱前缀派生，冲突加随机后缀）+ `login` 兼容邮箱登录
- `routes.py`：`POST /api/auth/email-code`、`POST /api/auth/register-email`
- `store.py`：`email_codes.json` 存储
- 前端 `AccountPage.tsx`：注册 tab 内"邮箱注册/用户名注册"双通道切换，验证码 60s 倒计时重发；登录框支持输入邮箱或用户名

## 验证（全绿）

- 新增 `tests/test_email_register.py` 31 项全过——内嵌 mini SMTP 服务器验证真实发信链路（含验证码落进邮件正文、限频、过期、防爆破、SMTP 不可达 502 回滚）
- 既有回归：membership 47、points_hook 19、mono_pauses、registry_order 全过
- 浏览器真机：tab 三页互通高亮正确；邮箱注册表单、未配置 SMTP 时错误提示正确
- `git revert aeb53b9` 可单独回滚本轮

## 启用邮箱注册（需要你做的）

QQ 邮箱网页版 → 设置 → 账号 → 开启 SMTP 服务 → 生成授权码 → `.env` 加：

```ini
SMTP_HOST=smtp.qq.com
SMTP_PORT=465
SMTP_USER=你的QQ号@qq.com
SMTP_PASS=授权码（不是QQ密码）
```

重启 backend 即生效。详细说明在 `webui-backend/MEMBERSHIP.md`「邮箱注册」节。
