# 播客工坊整改清单（2026-09-21 凌晨）

本次会话 commit：`58b273a`（登录态 401 修复）→ `1748122`（去保存预设）→ 本次（播客云引擎 + 并发 + 隔离 + 标题）。
测试账号、测试项目、测试音频已全部清理；本地库仅剩真实账号 diamondiamon。

---

## 一、双人播客走 302.ai 云引擎 ✅（已完成）

- 播客任务不再发往本地 tts-server，改为与单人配音**同一条适配层链路**（自建 → 302.ai → SiliconFlow → autodl.art，`TTS_ENGINE_PREFERRED` 调序）。`.env` 已配 `TTS_ENGINE_PREFERRED=indextts_302ai`，**无需开 SSH 隧道即可合成双播客**。
- 新增 `app/podcast_runner.py`：多音色（A/B 各自参考音频+语速）、行内 `[pause:N]` 后端精确切分、行间静音三类与原引擎逐条对齐（行级显式 > 末行 0 > 说话人切换 > 同行连续）。
- 段级变速 + 响度归一：每段 ffmpeg 管道处理（ebur128 测量 → 固定增益 → 限幅，目标 -16 LUFS，统一 24kHz）——与原播客引擎同参数；302.ai 不支持 speed，由后端补变速。ffmpeg 缺失时自动降级跳过（仅告警，不会让任务失败）。
- 真机验证：播客任务提交 → `engine=indextts_302ai` → 合成成功 → wav 产物 24kHz 单声道正常可播。

## 二、用户数据隔离 ✅（已完成，重点加固）

隔离在 `MEMBER_ENFORCE=1` 或 `MEMBER_REQUIRE_LOGIN=1` 任一开启时生效（你的 .env 两个都开了）。

| 数据 | 隔离方式 | 状态 |
|---|---|---|
| 项目存档 | 新增 `owner_id` 归属；列表/读取/更新/删除全部按归属过滤，越权一律 404；禁止覆盖他人同名存档 | ✅ 端到端 12 项断言全过 |
| 队列任务 | 登录态总是写入 `member_id`；查看/编辑/重试/取消/清空/拖拽排序全部按归属校验；未登录看队列 → 401 | ✅ 跨用户读取/取消/重试均 404 |
| 本地上传音色 | `voices_meta.json` 记录归属；列表只显示自己的；改名/删除校验归属 | ✅ |
| 收藏音色 | 按用户分文件存储 | ✅ |
| **TTS 服务侧音色库** | **共享池**（tts-server 无用户体系，预置音色 + 经 TTS 上传的音色全平台可见） | ⚠️ 架构限制，见下方"遗留项" |
| 术语表 | 当前全局共享（所有用户同一份） | ⚠️ 单用户阶段无碍 |

- **遗留数据已迁移**：`tools/migrate_owners.py` 已对 diamondiamon 执行，2 个旧项目、2 条旧任务、9 个本地上传音色全部划归，你的老项目都能正常看到。
- 无主新数据（今后管理员手工导入等）可用同一脚本划归：`python tools/migrate_owners.py <用户名> [--dry-run]`。

## 三、每用户并发 ✅（已完成）

- `USER_CONCURRENCY=3`（.env 可调，1–10）：**每个用户**可同时运行 3 个任务，不是全平台 3 个。全局另有硬上限 `USER_CONCURRENCY_GLOBAL=12` 防失控。
- 调度：FIFO，队首任务若所属用户并发已满则跳过看下一个（不让一个重度用户堵死别人）。
- 未登录/无会员环境退化为全局串行（上限 N），行为兼容旧版。
- 真机验证：同账号连发 3 个播客任务 → 同时 running → 全部 success。

## 四、段落顺序保证 ✅（已确认，代码层落实）

- 段落可并发合成（`TTS_CONCURRENCY=3`，任务内段级并发），但拼接用 `asyncio.gather` **保序**：结果顺序 = 提交顺序 = 文本顺序，**第 10 段先完成也不会跑到第 8 段前面**。失败段单独重试，成功段不重扣调用。

## 五、段级文件与"只重生成第 8 段"（答疑，未实现）

- **现状**：分段音频全程在内存中处理，拼接后只保留整篇 wav（`data/outputs/podcast_{任务id}.wav`），任务删除时一并清理。原 tts-server 版会留临时分段目录但无索引、不可复用。
- **未来方案**（已为你验证过可行性，说一声就做）：按 `文本+音色+情绪+语速` 哈希做段级缓存（`data/segments/`），改第 8 段重新提交时，1/2/.../7/9/10 段命中缓存直接复用，只真合成第 8 段——302.ai 按段计费下能省 90% 费用。前端再加"第 N 段单独重生成"按钮即可。

## 六、页面标题 ✅（已完成）

- 浏览器标签页标题统一为「播客工坊 · Podcast Studio」，所有 tab 不再变化。

---

## 遗留项与建议（不紧急）

1. **TTS 侧音色共享池**：要彻底隔离需给 tts-server 加用户体系（或音色全部收编到 backend 管理），工作量一天左右，建议等有真实多用户后再做。
2. **术语表**：目前全局共享，多用户后建议 per-user。
3. **音频文件访问**：`<audio>` 标签无法带登录头，任务音频 URL 未做请求级鉴权（任务 id 随机不可枚举，风险低）；要强隔离可换短时签名 URL。
4. **段级缓存**（见第五节）：强烈推荐作为下一个功能点，省钱利器。
5. 你本地 backend 是我测试后重启的新代码（后台运行中）；**服务器部署时直接拉最新代码重启即可**，`.env` 里 `TTS_ENGINE_PREFERRED=indextts_302ai` 保持不变。
