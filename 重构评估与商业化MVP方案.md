# podcast-webui 重构评估与商业化 MVP 方案

> 2026-09-16 · 基于对三层代码的完整走查 + MiniMax / 讯飞配音竞品调研
> **一期定论（2026-09-16 与用户确认）**：不推倒重写，走"原地分层改造"。**一期主引擎 = 自建服务器（IndexTTS2）+ autodl.art API（IndexTTS2 工作流）**——两者同底座、实现最简单，且自建成本低（RTF 实测 0.6~0.9）；MiniMax/CosyVoice 等商用 API 后期再补，但**引擎适配层现在就设计好**（统一接口 + 音色目录惰性注册 + 8 标签情感映射），后补即插即用。autodl.art 单次 ≤2048 字符的分片控制在 backend 统一实现。

---

## 一、三层架构现状（已走查确认）

```
┌─────────────────────┐   ┌──────────────────────────┐   ┌───────────────────────────┐
│ webui-frontend      │   │ webui-backend            │   │ tts-server (GPU)          │
│ React18+Vite+TS     │──▶│ FastAPI 单文件 1647 行    │──▶│ FastAPI 577行 + 引擎 606行 │
│ 无路由/无鉴权        │   │ 任务队列+持久化(JSON文件)  │   │ IndexTTS2 推理+音频后处理  │
│ :6008  /api 代理     │   │ :3001  httpx 转发         │   │ :8000  模型锁串行推理      │
└─────────────────────┘   └──────────────────────────┘   └───────────────────────────┘
```

**核心调用链**（生成一条播客）：
`OutputPanel → App.tsx:265 → POST /api/queue/submit → webui-backend server.py:1365 入队 → _process_queue(:1235) 术语替换 → POST {TTS}/api/podcast (:1267) → tts-server server.py:405 后台线程 → podcast_engine.py:448 逐行 infer + 变速/响度归一 + 拼接 → backend 每 1.5s 轮询 :1286 → 成功后前端经 /api/podcast/audio/:697 中转播放`

**关键事实**：
1. **tts-server 的 `speaker` 只是 voices 字典的任意 key**（podcast_engine.py:116）——"单音色"不需要动引擎，`voices={"A": 某音色}` + 所有行 speaker="A" 即天然支持。
2. `/api/synthesize`（tts-server server.py:342）**已经是单音色+文本+情感的同步合成端点**，单篇配音模式后端能力约 80% 现成。
3. 全链路无登录/鉴权/CORS 限制（后端 `allow_origins=["*"]`），纯单用户工具设计。
4. 持久化全是 JSON 文件，无数据库；tts-server 任务纯内存（重启即丢）。
5. GPU 侧 `_model_lock` 全局锁，**同一时刻只能跑一个推理**，并发能力 = 队列。
6. tts-server-2.5 是整目录复制的平行实现（端口 8001），同一份前端参数在 2.5 上被静默忽略——"复制目录做版本隔离"已到极限。

### 三层强耦合点 vs 可复用资产

| | 强耦合"双人"的部分（要动） | 通用可复用（不动/微调） |
|---|---|---|
| 前端 | `speaker: "A"\|"B"` 字面量类型（types/index.ts:42）、`voices:{A;B}`（:64）、ScriptEditor 奇偶行分配、App.tsx speakerSpeeds 两键 | 音色管理全链路（上传/收藏/预设）、VoicePicker、EmotionEditor、队列面板、ui.tsx 组件库 |
| backend | `ProjectModel.voices` A/B 默认值（:70）、文本导入按 `"A:"/"B:"` 解析（:789-822） | 队列框架（持久化/重试/重排/断连容错）、术语表、音频代理 |
| tts-server | **零耦合** | 全部：合成、情感四模式、变速、响度归一、拼接、静音控制 |

---

## 二、IndexTTS2 底座能力 vs 竞品功能对照

竞品调研结论：MiniMax 主打 **标签驱动**（情绪 8 种、`<#>` 停顿、语气词快捷输入、5000 字/次、长文 20 万字、40+ 语言、声贝按字符计费 ¥2~3.5/万字符、音色克隆 ¥9.9/个）；讯飞配音主打 **编辑器工作台**（多音字/数字读法/停顿 0.5-2s/换气/连读/局部变速变调音量/背景音乐/多人配音，200+ 音色，单次 2 万字符，声币计费约 0.5 元/百字，导出 MP3）。

| 竞品功能 | IndexTTS2 能否支持 | 方式与代价 |
|---|---|---|
| 选音色+输入文本生成 | ✅ 现成 | `/api/synthesize` 已具备 |
| 情绪标签（开心/难过…8 种） | ✅ 大部分现成 | EmotionConfig mode=2（8 维向量）/mode=3（文本描述）；需做"标签→参数"映射表（约 30 行） |
| 分段打不同情绪标签 | ✅ 现成 | 每行独立 emotion，引擎已支持 |
| 语速调节 | ✅ 现成 | 2.0 ffmpeg atempo；2.5 模型内生 duration_factor |
| **音色克隆（上传声音定制音色）** | ✅ **这是 IndexTTS2 的强项，比部分竞品还好** | 参考音频即音色，现有 voices 上传链路直接就是克隆功能，可做卖点 |
| 停顿插入（0.5s/1s/2s） | ⚠️ 需自建 | 文本里插 `<#>` 标记 → 预处理拆分 + `_concatenate_wav_segments` 插静音帧，约 50-80 行 |
| 段间静音控制 | ✅ 现成 | SilenceConfig 已支持三级静音 |
| 多音字标注 | ⚠️ 弱（已实测） | 拼音标注采纳率约 1/6 是软提示；生产走术语表换词。讯飞式"点击改音"交互可做，但后端靠术语表而非拼音标注 |
| 数字/年份读法 | ✅ 现成 | `_sanitize_text` 已处理年份逐位、时分 |
| 局部变速/变调/音量 | ⚠️ 可做 | ffmpeg 后处理按段实现，工作量中等，MVP 可砍 |
| 背景音乐混音 | ⚠️ 可做 | ffmpeg amix，约 50 行，MVP+1 |
| 长文本（10 万字级） | ⚠️ 受限 | 无硬限制但串行推理慢；需按段切+队列，产出时长可控（讯飞也是分段的） |
| 40+ 语言 | ❌ 不支持 | 2.0 中文为主、2.5 有 lang 字段但 ZH 为主。**短期放弃多语言，聚焦中文** |
| 多人对话 | ✅ 现成 | 就是现有播客模式 |
| 秒级响应/高并发 | ❌ **最大短板** | 单 GPU 模型锁串行。RTF 需实测（估计 4090 上 1 秒音频 1~3 秒生成），商业化必须靠排队+分段并行度设计 |

**结论**：竞品功能里"能直接抄"的占大多数——UI/交互照着做没问题；**抄不了的是他们的推理吞吐和多语言**，这两项不硬碰，靠"排队生成 + 聚焦中文 + 音色克隆作差异化卖点"绕开。IndexTTS2 作底座**成立**，尤其零样本音色克隆是相对讯飞/MiniMax 低价克隆的真实优势。

---

## 三、整改 vs 重写：评估结论

| 方案 | 工作量 | 风险 | 判断 |
|---|---|---|---|
| A. 全新重写（新架构新仓库） | 4-6 周起 | 队列容错/断连恢复/响度归一这些**已踩坑调好的逻辑全部重写重测**；双人播客功能回归风险 | ❌ 不推荐。现有 60% 代码是可直接复用的资产 |
| B. 原代码上直接加功能 | 最快 | backend 1647 行单文件继续膨胀；A/B 双人类型散布前端，单音色功能塞进去会很别扭；商业化模块（用户/计费）没地方放 | ⚠️ 不推荐原样硬塞 |
| **C. 原地分层改造（推荐）** | 中 | 可控 | ✅ 见下 |

**方案 C 要点**：
1. **tts-server 基本不动**（单音色零改动；标签映射 +30 行；停顿标记 +60 行）。补一个"引擎适配层"抽象，把 2.0/2.5 的差异收敛到配置，终止"复制目录"模式。
2. **backend 拆模块 + 加商业化层**：`server.py` 拆成 routes/{voices,queue,projects,glossary,auth,billing,tasks}.py，逻辑不动只搬家；新增 SQLite（先不上 Postgres，单机够用）存用户/积分/任务；JWT 登录中间件。
3. **前端加"配音模式"**：不动现有播客模式，顶部加模式切换；新增 MonoEditor（单音色+分段+标签）是最大新增件（约 300-400 行）；`speaker` 类型从 `"A"|"B"` 放宽为 `string`，播客模式语义不变。
4. 类型收紧：client.ts 的 `any[]` 补类型，顺带清理死代码（`/api/podcast/generate` 直连模式、SSE 订阅、OutputPanel 死状态）。

---

## 四、商业化 MVP 路线（尽快上线验证市场）

### MVP 范围（目标：2-3 周可上线收用户）

| 阶段 | 内容 | 验收 |
|---|---|---|
| **G0：底座夯实**（4-5 天） | backend 拆模块（行为不变）；**引擎适配层落地**：`indextts_local`（自建）+ `indextts_art`（autodl.art）两个适配器，`ensure_voice/synthesize` 接口定型，MiniMax/CosyVoice 仅留 Protocol 占位不实现；**2048 字分片器**（长文本贪心切满片）；tts-server 加标签映射+停顿标记；计费系数按 RTF 0.6~0.9 定 | 同一份参数在两个引擎都能出音；自建不可达时自动切 autodl.art |
| **G1：单音色配音模式**（4-5 天） | 前端模式切换 + MonoEditor（选音色/贴文本/分段打情绪标签/语速/试听）；音色上传走 `ensure_voice`（参考音频型目录）；复用队列提交 | 从选音色到出音频全流程可用，长文本分段合成，引擎可配置切换 |
| **G2：用户与计费**（4-5 天） | 注册/登录（JWT）、积分制（按字符预扣，失败退回）、任务历史归属用户、SQLite | 双用户隔离；积分扣减/退款正确 |
| **G3：充值与上线**（3-4 天） | 支付先走**手动充值/兑换码**（个人收款渠道合规问题多，先用最土的方式验证付费意愿）；部署 + 压测并发排队体验 | 公网可访问，能收到第一笔付费 |

**MVP 明确不做**：多语言、局部变速变调、背景音乐、音色克隆的付费门槛（MVP 先免费开放作获客点）、移动端适配、微信/支付宝自动支付。

### 商业模式参考（按竞品定价锚定）

- MiniMax API 成本基准：HD ¥3.5/万字符、Turbo ¥2/万字符、克隆 ¥9.9/音色
- 讯飞零售价：约 ¥0.5/百字（=¥50/万字符，API 成本价的 10-25 倍），会员 1999 元/年 10 万字
- **我们的定价空间**：RTF 实测 0.6~0.9 ⇒ 自建 GPU 时耗成本约 ¥1.1/万汉字，可定 ¥5-15/万字符区间 + 会员制，毛利空间厚，用"音色克隆免费/低价"打差异化。自持 GPU 吞吐上限（单卡串行）仍是并发硬约束。

### 风险清单

1. **单 GPU 并发是硬天花板**：多用户同时提交只能排队，需在 UI 上明示排队位置（现有队列面板已有此能力，复用即可）；用户量上来后需第二张卡/实例。
2. **自建实例"开不了机"风险**（见第六节）：关机后再开机可能因平台无卡可用而失败，对商用平台是可用性硬伤，必须有预案。
3. **多音字纠错体验弱于讯飞**：拼音标注是软提示（实测采纳率 1/6），产品层用"术语表"兜底，不要在 UI 上承诺"精准多音字"。
4. **合规**：音色克隆涉授权（要求用户声明素材版权）；生成内容审核（参照讯飞 12h 人工审可先做关键词过滤）；商用声明。
5. **个人支付渠道**：微信/支付宝官方商户号对个人不开放，先兑换码/手动，验证付费意愿后再办个体户/公司进官方通道。

---

## 六、算力底座补充评估：自建实例 vs autodl.art 托管 API（2026-09-16 补）

前次（2026-09-14）已做过 autodl.art IndexTTS2 接入讨论，产物在 `podcast-webui/tools/test_autodl_indextts2.py`（提交 `https://autodl.art/api/v1/comfyui/comfyui_workflow/indextts2-v1` → 轮询 result → 下载音频的隔离冒烟脚本；配套 `autodl_body.json` 请求体模板**目前是空文件，冒烟尚未实际跑通**）。这里把"算力从哪来"作为架构问题正式并入评估。

### 三种算力来源对比

| | A. 自建 AutoDL 容器实例（现状） | B. autodl.art 托管工作流 API | C. 商用 TTS API（MiniMax 等） |
|---|---|---|---|
| 可用性 | ❌ **关机后再开机可能"无卡可开"**；连续关机 15 天实例被释放；开机即依赖指定区域有卡 | ✅ 平台侧调度，官方口径"显卡资源充足、不抢卡"，无开机问题 | ✅ SLA 最好 |
| 计费 | 按小时开机：4090 ¥1.98/h（≈¥1430/月常开）、3090 ¥1.32/h（≈¥950/月） | 按秒计费、关机即停、排队不计费；indextts2 工作流单价未公开，需登录控制台确认 | ¥2~3.5/万字符，按量 |
| 能力面 | **完全可控**：响度归一、变速、拼接、术语表、采样参数、token 诊断全部现成 | 受限于工作流暴露的字段（单段文本+参考音频）；后处理链全没有，需搬回 backend 用 ffmpeg 自建；长文本要多次调用（成本优化方向 9-14 已讨论过：按说话人合并文本） | 参数丰富但音色风格不可控、克隆有平台审核 |
| 改造成本 | 0（现成的就是它） | backend 加一个"引擎适配器"（方案 C 的引擎适配层正好接住）；冒烟脚本已有，只差模板和实测 | 同左，加适配器即可 |
| 定位 | **主链路** | **溢出算力 + 宕机备援** | 最后兜底（可选） |

### 针对"关机后开不了机"的应对预案（按优先级）

1. **尽量常开**：商用验证期直接 4090 常开（约 ¥1430/月），把"开机"这个动作从日常运营里消掉；计费系数（G0 实测 RTF）要按这个成本核算。
2. **引擎适配层 + autodl.art 备援通道**：方案 C 本来就要抽引擎适配层，顺势把 autodl.art 做成第二引擎——backend 收单后先试自建 tts-server `/api/health`，不可达或开机失败时自动切托管 API，任务照常进队列，用户无感。**这是唯一能真正兜住可用性的方案**。注意托管引擎没有响度归一/拼接，需把 `_apply_speed`/`_concatenate_wav_segments` 这两个纯函数从 podcast_engine 挪到 backend 侧共用（它们本来就是与角色无关的纯函数，可搬）。
3. **业务层降级**：双引擎都不可用时，前端展示"算力维护中"，队列暂停接单（现成 queue/paused 机制），恢复后自动续跑——现有断连容错（SYNCING/interrupted 状态机）已覆盖一半。
4. **运维细节**：AutoDL 提交后台 API 或脚本定时探活；实例数据全在数据盘 + start_autodl.sh 一键拉起，保证"换一台卡也能 10 分钟内恢复"。

### 决策建议（2026-09-16 终版：一期 = 自建主 + autodl.art 备）

> 用户确认的一期路线：**两个引擎都是 IndexTTS2**（自建 tts-server 2.0 + autodl.art indextts2-v1 工作流），实现最简单；MiniMax/CosyVoice 等商用 API 后期再补，但架构上现在就把适配层、音色目录、8 标签情感映射设计到位（见第七节），后补即插即用。

- **主引擎 = 自建 tts-server（IndexTTS 2.0，非 2.5）**：RTF 实测 0.6~0.9（比实时还快），4090 上单集 15 分钟播客 GPU 成本约 ¥0.30~0.45，参数全可控，开发调试也靠它。
- **备援引擎 = autodl.art API**（已实测调通）：自建"无卡可开"/宕机时自动切换；**单次 ≤2048 字符的硬限制在 backend 分片器统一控制**——播客逐段天然小于 2048 无需处理，单音色长文本按 2048 贪心切满片（按次计费，切得越满越省）。
- **可用性兜底**：双引擎都不可用 → 前端"算力维护中"+ 队列暂停接单（复用现有 paused/SYNCING 机制）；商用 API 适配器作为后期扩容路径，接口预留不实现。
- **自建可用性缓解**（一期就做，成本极低）：数据全在数据盘 + start_autodl.sh 一键拉起，保证换卡 10 分钟内恢复；商用时段建议尽量常开或用 AutoDL 探活脚本。

**autodl.art 已确认事实（工具已就绪）**：计费**按次提交、单次 ≤2048 字符**。请求体字段：8 个情绪滑杆（emo_happy/sad/angry/afraid/disgusted/melancholic/surprised/calm）+ `emo_control_method` + `prompt_simple`（参考音频 base64 data URI）+ `prompt_text`。`tools/autodl_body.json` 模板已落盘、`test_autodl_indextts2.py` 已支持本地音频自动转 base64，**用户已实测调通**。

---

## 七、商用 TTS API 备选清单与兼容性评估（2026-09-16 补）

前提：商用 API 的定位是**备援 + 容量溢出**（未来可加多语言），不是主链路。选型时最关心的"兼容性"落点是：能不能塞进同一个引擎适配层协议——`(text, 音色, 情感, 语速) → 音频字节`，且后处理（响度归一/拼接）统一在 backend 做完再交付。

| 服务商 | 模型/接口 | 价格（公开口径） | 情感控制 | 音色/克隆 | 单次文本上限 | 兼容性评价 |
|---|---|---|---|---|---|---|
| **MiniMax** | speech-2.8-hd/turbo T2A API | hd ¥3.5/万字符、turbo ¥2/万字符；克隆 ¥9.9/音色 | 8 情绪标签、选段应用、`<#>` 停顿 | 预置音色库 + 克隆 | 实时 5000 字符；异步长文 20 万 | ★★★ 长文异步任务制与我们的队列模型最像；8 情绪与我们 8 维对齐最好 |
| **阿里云百炼 CosyVoice** | cosyvoice-v3.x / qwen3-tts | CosyVoice ¥2/万字符（1 汉字=2 字符）；qwen3-tts 按 token | 自然语言 instruction（"用高兴的语气说"） | 预置 + 参考音频零样本克隆 | CosyVoice 2 万/次 | ★★★ 大厂 SLA + 参考音频即克隆，与我们 voices 机制同构；instruction 式情感需映射 |
| **硅基流动 SiliconFlow** | CosyVoice2-0.5B / fishaudio / GPT-SoVITS / MOSS-TTSD | 未核实（控制台确认） | 自然语言/富文本细粒度 | 预置音色 + 上传参考音频得 voice id | — | ★★ **OpenAI 兼容 `/v1/audio/speech` 接口，接入代码量最小**；注意：**没有 IndexTTS2**，音色风格 ≠ 我们的底座 |
| **火山引擎（豆包）** | 豆包语音大模型 TTS | 未核实（控制台确认） | 情感/风格参数丰富 | 声音复刻 2.0 | 支持流式 | ★★ 大厂质量好，但需企业认证流程较重 |
| **腾讯云 / 百度千帆 / 讯飞开放平台** | 传统大模型 TTS | 约 ¥1-3/万字符量级（各自控制台确认） | SSML 或参数式 | 预置为主，克隆另购 | 各异 | ★ 传统 TTS 自然度不如上面几家，仅作目录备选 |

**兼容性设计结论**（写进引擎适配层的统一约束）：
1. 统一情感表示用 **8 标签**（开心/难过/生气/害怕/厌恶/惊讶/中性/生动）——MiniMax 官方就是这 8 个、讯飞是 8 情绪、IndexTTS2 的 8 维向量与 autodl.art 的 8 个滑杆都一一对应。各引擎在适配器里做"标签→自家参数"映射。
2. 计费口径天然对齐：商用 API 全部按字符计费（汉字=2 字符），我们积分制按字符预扣的规则可以直接沿用；自建引擎按实测 RTF 折算一个等效单价即可统一结算。
3. 音频交付统一在 backend 收口：商用 API 返回 mp3/URL、自建返回 24kHz WAV，适配层统一转码 + 响度归一后再落盘，前端永远拿到同一种格式。
4. 长文本策略统一走**分片 + 队列**：MiniMax 异步 20 万字、CosyVoice 2 万/次、autodl.art 2048 字/次、自建按段——反正都在 backend 分片，引擎差异被屏蔽。

**建议顺序（2026-09-16 终版）**：一期主引擎 = 自建 + autodl.art（同为 IndexTTS2，实现最简）；本节适配层设计与下方核实结果作为**后期补商用 API 的预留蓝图**——接口定义、音色目录、8 标签映射现在就按此实现，届时只写适配器。

### 商用 API 两个硬要求的核实结果（2026-09-16 官方文档验证）

| 硬要求 | MiniMax | 阿里百炼 CosyVoice |
|---|---|---|
| 上传参考音频克隆 | ✅ `files/upload` → `voice_clone`（≥10s、≤5min、≤20MB；复刻费 ¥9.9 在**首次用该音色合成时**才收；音色 7 天不用会被删） | ✅ `voice-enrollment` 声音复刻 API（建/查/改/删全生命周期管理） |
| 情感控制 | ✅ t2a_v2 请求带 `emotion` 参数（happy/sad/angry 等 8 种）+ `speed` | ✅ `instruction` 自然语言情感/方言/风格控制 |
| 播客逐段合成 | ✅ 官方就有 AI 播客示例（t2a_v2 每段一调，按字符计费，无按次费） | ✅ 每段一调按字符计费 |
| 准入 | 音色克隆需个人/企业认证（提前办） | 阿里云账号即可 |

### 引擎适配层统一设计（解决"怎么统一"）

```python
class TTSEngine(Protocol):
    async def ensure_voice(self, ref_audio: bytes, mime: str, key: str) -> str:
        """上传参考音频 → 引擎侧音色句柄。key=音频哈希，做本地缓存避免重复克隆扣费。"""
    async def synthesize(self, text: str, voice_id: str,
                         emotion: str | None, speed: float = 1.0) -> bytes:
        """合成单段 → 音频字节。分片由 backend 队列层统一做，引擎只管单段。"""
```

两种音色模式在适配层内消化：
- **注册制**（MiniMax/百炼/硅基流动）：`ensure_voice` 调注册 API 拿 voice_id，按音频哈希缓存。注意 MiniMax 两个坑：复刻费首次合成时收（缓存命中即不再扣）；音色 7 天不用被删 ⇒ 缓存失效时静默重注册即可。
- **随请求带参考音频**（IndexTTS2/autodl.art）：`ensure_voice` 只做本地缓存（哈希→文件路径），`synthesize` 时带上。
- **情感统一 8 标签**：MiniMax `emotion` 参数、autodl.art 8 滑杆、IndexTTS2 8 维向量直接映射；CosyVoice 由标签生成 instruction 文案。映射表 30 行内。
- **音频收口**：MiniMax 返回 mp3(hex)/百炼返回 URL/自建返回 24kHz WAV → 统一转码 + 响度归一后落盘，前端永远拿同一种格式。

### 预置音色策略：统一音色目录 + 惰性注册（2026-09-16 补）

问题：各引擎预置音色互不相通；我们的内置预设音色（preset-voices/，参考音频型）难道每家都要克隆一遍？MiniMax 每个音色 ¥9.9？

**核心方案：音色目录独立于引擎，参考音频是唯一真源，按引擎惰性注册。**

```python
Voice {                       # 平台自己的音色目录，引擎无关
  id, name, tags,             # 性别/风格/情感标签
  ref_audio: 本地参考音频,     # ← 唯一真源（preset-voices/ 里那 66 个就是它）
  engine_handles: {           # 惰性填充：第一次被某引擎实际使用时才注册
    "cosyvoice": "voice_xxx", # 注册免费
    "minimax":   "voice_yyy", # 首次合成时收 ¥9.9，一次性
    "indextts":  "/path/a.mp3" # 本地路径，零成本
  }
}
```

- `ensure_voice(key=音频哈希)` 的缓存就落在 `engine_handles`：**不预付、不批量克隆**。音色被用户选中并真正合成时才向对应引擎注册一次，此后零边际成本。新增音色 = 上传一段参考音频，永远不用先交钱。
- **成本账**：¥9.9 是"单音色 × 单引擎"的一次性费用。50 个预设音色就算全部在 MiniMax 激活也才 ¥495 一次性，摊进后续所有调用可忽略。真正的省钱路由是：**预设音色默认走 CosyVoice（注册不单收费，计费页只有合成按字符项；音色数量上限待控制台确认）**；MiniMax 留给用户定制音色（值得收 ¥9.9，可向用户转嫁，竞品也这么收）和高保真档。
- **两类音色条目并存**：① `ref_audio` 型——跨引擎可迁移（我们 66 个预设 + 用户上传），走惰性注册；② `engine_native` 型——引擎官方预置音色库（MiniMax 数百个、CosyVoice 也有），**免费、已授权、不用克隆**，映射进我们目录打上标签即可，缺点是不可迁移到其他引擎。前端统一展示，条目上标 capability。这样零成本就能先把目录填满。
- **两个坑**：① MiniMax 克隆音色 7 天不用会被删 ⇒ `engine_handles` 标记易失，缓存失效时静默重注册（CosyVoice 免费随便重来；MiniMax 重注册会再收 ¥9.9，所以 MiniMax 上只放高频预设和用户定制音色，天然复用率高）；② 音色列表的**试听也按字符计费** ⇒ 试听片段限字数（如 ≤50 字）+ 同一音色试听音频服务端缓存一次，所有用户共享。

### 播客单集成本测算（15 分钟 ≈ 4000 汉字 ≈ 50 段对话）

| 引擎 | 计费方式 | 单集成本 | 折算单价 |
|---|---|---|---|
| **自建 4090（一期主力）** | ¥1.98/h，RTF 实测 0.6~0.9 | 900s 音频 × RTF = 9~13.5 分钟 GPU ≈ **¥0.30~0.45** | **≈¥1.1/万汉字**，三家最低 |
| MiniMax turbo（后期） | ¥2/万字符（汉字=2字符） | ≈¥1.6（hd ≈¥2.8） | ¥4/万汉字 |
| 阿里百炼 CosyVoice（后期） | ¥2/万字符 | ≈¥1.6 | ¥4/万汉字 |
| autodl.art（一期备援） | 按次（≤2048字/次，约 ¥0.02/次） | 50 段 ≈ ¥1 | ≈¥5-12/万汉字，段越多越亏，只作备援 |

**结论**：RTF 0.6~0.9 实测后，自建单集成本约为商用 API 的 1/4、autodl.art 的 1/3 —— 一期"自建为主 + autodl.art 备援"在成本上完全站得住；自建的风险只剩可用性（无卡可开），靠"数据盘 + 一键拉起 + 常开策略 + autodl.art 自动切换"缓解。后期用户量上涨、排队体验撑不住时，再按第七节蓝图补 MiniMax/CosyVoice 做容量溢出。

---

## 五、下一步（待确认后开工）

1. 确认方案 C 与 MVP 范围 → 进入 G0
2. G0 第一件事：实测 RTF（不同长度文本 × 音色 × 2.0/2.5 引擎），产出计费系数表
3. G0 并行：补齐 `tools/autodl_body.json` 请求体模板，跑通 `test_autodl_indextts2.py` 冒烟 → 评估 autodl.art 作为备援引擎的可行性（效果/耗时/单价）

---

## 进度记录

### 2026-09-16 · G0 完成并真机验证 + G1 本地完成

- **G0 三项全部完成**：backend 拆模块（app/ 包，行为不变）、引擎适配层（indextts_local + indextts_art + 2048 分片器）、tts-server 停顿标记 `[pause:秒]`/`<#>`。**停顿标记已在真机验证通过**（用户实测停 1 秒达效）。
- **G1（单音色配音模式）本地完成**：
  - 后端：`app/mono_runner.py`（走引擎适配层逐段合成，任务内固定单引擎防混流，backend 侧 wave 拼接 + 段后静音，落盘 `data/outputs/`）；`/api/queue/submit` 加 `kind` 字段（podcast/mono），process_queue 按 kind 分流；新增 `GET /api/mono/audio/{task_id}`；mono 任务重启后标记 INTERRUPTED 可重试。
  - 前端：`App.tsx` 模式切换（双人播客 / 单人配音，localStorage 记忆）；`MonoVoiceCard`（音色选择/上传/试听/语速）；`MonoEditor`（导入文本按行拆段、每段情绪标签 10 选项 + 段后停顿秒数、字数统计）；QueuePanel 加「配音」徽标；配音草稿 localStorage 持久化。
  - 验证：mono 单测（拼接/情绪映射/注册表）全过；mock TTS 端到端（提交→合成→拼接→`/api/mono/audio` 取回，3×0.6s+0.5s 停顿=2.3s 精确）；无 kind 老任务回归走播客路径正常；tsc + vite build 通过。
  - **待真机回归**：部署拆分后 backend + 前端到 AutoDL，实测配音模式全流程。
